{
  "name": "single-spa",
  "version": "1.0.0",
  "scripts": {
    "start-root": "cd cyber-root && npm install && serve -s -l 4200",
    "start-grid": "cd cyber-grid && npm install && npm start",
    "start-text": "cd cyber-text && npm install && npm start",
    "start-image": "cd cyber-image && npm install && npm start",
    "start-list": "cd cyber-list && npm install && npm start"
  },
  "devDependencies": {
    "serve": "^12.0.0"
  }
}
